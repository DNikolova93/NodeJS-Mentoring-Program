
import authConfig from './authConfig';
import configApp from './baseConfig';
import errorConfigApp from './errorConfig';

export default {
  baseConfig: configApp,
  errorConfig: errorConfigApp,
  authConfig,
};
